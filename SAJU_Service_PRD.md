# 사주 풀이 서비스 — MVP 기획서

| 항목 | 내용 |
| --- | --- |
| 문서명 | 사주 풀이 서비스 MVP 기획서 |
| 버전 | **v1.1-MVP** |
| 작성일 | 2026.09.09 |
| 변경 이력 | v1.1 — §1.4 "0원 배포 분기점" 신설(Day 4 체크포인트), 십신·신살 용어 사전을 고도화 리스트에서 MVP로 편입<br>v1.0 — 최초 MVP 슬림화 (22일 기획서 → 7일) |
| 작성자 | 윤민지 |
| 개발 방식 | Vite + React 직접 개발 (Claude Code 페어 프로그래밍) |
| 배포 | GitHub 레포 → Netlify 자동 배포 |
| 목표 기간 | **약 7일** |

> 이 문서는 **처음 만들어질 한 벌**만 정의한다. 뺀 것들은 §9 고도화 리스트에 이유와 함께 남겨뒀다.

---

## 1. 무엇을 만드는가

생년월일시를 입력하면 **만세력(사주 8글자)을 계산해 보여주고**, 그 계산 결과를 근거로 **AI가 타고난 성향과 기질을 풀어주는** 웹사이트.

### 1.1 절대 원칙 ⭐

> **계산은 라이브러리가, 해석은 AI가.** 이 경계를 넘지 않는다.

| 영역 | 담당 | 이유 |
| --- | --- | --- |
| 사주 8글자, 오행, 십신, 신살 | **만세력 라이브러리** | 천문 계산이라 정답이 하나. AI에게 맡기면 틀린다 |
| 계산 결과를 사람의 말로 풀어 쓰기 | **AI** | 문장 만드는 일은 AI가 잘한다 |
| 없는 정보를 지어내기 | **금지** | 프롬프트로 차단 (§6.2) |

이 원칙이 무너지면 제품이 무의미해진다. 나머지는 다 타협해도 이건 안 된다.

### 1.2 만드는 것 / 안 만드는 것

**만든다**

- 입력 화면 (이름/닉네임, 생년월일시, 양·음력, 시간 미상, 출생지, 성별)
- 만세력 결과 (8글자, 오행 분포, 십신, 신살)
- **십신·신살 용어 사전** — 이름을 탭하면 한 줄 설명 (§1.4 참고)
- AI 해석 1종 — **기본 성향 및 기질**, 항목별 5개
- 같은 입력이면 같은 결과 (브라우저 저장)
- **모바일 우선 반응형** — 폰에서 URL로 접속해 쓰는 게 기본

**안 만든다** → 전부 §9로

회원가입·로그인·결제, 조회 기록 목록, 연애·직업·재물 등 다른 해석, 대운·세운 표시, 궁합, 별자리, 해외 출생지, 지장간 표시.

### 1.3 완성 기준

| 기준 | 목표 |
| --- | --- |
| 계산 정확성 | 검증 케이스 15건이 기존 만세력 사이트와 일치 |
| 일관성 | 같은 입력 재조회 시 같은 해석 |
| 모바일 | 실제 폰에서 전체 흐름 통과, 가로 스크롤 0 |
| 속도 | 만세력 표 즉시 / 해석 첫 글자 3초 이내 |

### 1.4 0원 배포 분기점 ⭐

> **§7 로드맵의 Day 4 끝에서 배포하면, 이 서비스는 완전히 0원으로 돈다.**

서버 함수가 하나도 없기 때문이다. AI 해석(§6)만 서버(Netlify Function)와 API 과금이 필요하고, 그 앞 단계인 만세력·오행·십신·신살은 브라우저 안에서 `ssaju`가 전부 계산한다.

| 배포 시점 | 제공 기능 | 비용 |
| --- | --- | --- |
| **Day 4 끝** | 만세력 8글자 + 오행 + 십신 + 신살 + **용어 사전** | **0원** (서버 없음) |
| Day 7 (전체 완성) | 위 전부 + AI 해석 | 캐싱 덕에 사실상 무료에 가까움 (§6.5 참고) |

**Day 4 버전을 "그냥 계산기"로 만들지 않기 위해, 원래 §9(고도화)에 있던 용어 사전을 MVP로 끌어왔다.** AI 해석이 없으면 `정재` `도화살` 같은 이름만 덩그러니 남아 아는 사람만 쓸 수 있는 화면이 되기 때문이다. 탭하면 한 줄 설명이 뜨는 사전(§5.3 [4][5])이 있어야 Day 4 버전 단독으로도 완결성이 있다.

**Day 4에서 멈춰도 되고, 그대로 Day 5로 넘어가 AI를 붙여도 된다.** 순서는 바뀌지 않는다 — 배포는 그 사이 아무 시점에나 한 번 해보고, 계속 만들지 말지는 그때 정하면 된다.

---

## 2. 용어 사전 (쉬운 말)

### 2.1 기술

| 용어 | 쉬운 말 |
| --- | --- |
| **Vite + React** | 화면 만드는 도구 묶음. 서버 없이 브라우저에서 도는 사이트를 빠르게 만든다 |
| **Netlify** | 만든 사이트를 인터넷에 올려주는 서비스. 깃허브에 코드를 올리면 알아서 배포해준다 |
| **Netlify Function** | Netlify가 대신 돌려주는 아주 작은 서버 조각. 요청이 올 때만 잠깐 켜진다 |
| **Messages API** | Claude에게 "이 질문에 답해줘"라고 HTTPS 요청 한 번 보내는 방식. 가장 단순한 형태 |
| **API 키** | Claude를 쓸 때 "나야"라고 알리는 비밀 문자열. 쓴 만큼 요금이 나간다 |
| **환경변수** | API 키처럼 코드에 직접 적으면 안 되는 값을 코드 밖에 따로 두는 것 |
| **스트리밍** | 답변이 다 만들어질 때까지 기다리지 않고, 만들어지는 대로 한 글자씩 흘려보내는 것 |
| **localStorage** | 브라우저가 기기 안에 데이터를 저장해두는 공간. 서버 없이 기억할 수 있다 |

### 2.2 사주

| 용어 | 쉬운 말 |
| --- | --- |
| **만세력** | 날짜를 사주 글자로 바꿔주는 달력표 |
| **사주팔자** | 태어난 연·월·일·시를 각각 2글자로 표현한 것. 4기둥 × 2글자 = 8글자 |
| **일간** | 일주의 위 글자. **나 자신**을 상징하며 모든 해석의 기준점 |
| **오행** | 목·화·토·금·수 5가지 기운. 무엇이 많고 없는지가 성향의 뼈대 |
| **십신** | 일간 기준으로 나머지 글자가 나와 어떤 관계인지 붙인 이름 (비견·식신·정관 등) |
| **신살** | 특정 글자 조합에 붙는 별칭 (도화살·역마살·천을귀인 등). 특성 태그에 가깝다 |
| **절기** | 계절을 24개로 나눈 기준점. **사주에서 연·월은 절기로 바뀐다** (1월 1일이 아님) |
| **진태양시** | 실제 태양 위치 기준 시각. 한국 표준시와 20~34분 차이가 난다 |

---

## 3. 기술 구조

### 3.1 핵심 판단 — 서버가 거의 필요 없다

만세력 라이브러리 `ssaju`는 **의존성 0의 순수 JavaScript**라 **브라우저에서 그대로 돌아간다.** 즉 사주 계산은 서버 없이 끝난다.

서버가 필요한 건 **AI 해석 딱 하나**다. API 키를 브라우저에 노출할 수 없기 때문이다.

```
[휴대폰 브라우저]
   │ ① 생년월일시 입력
   │ ② 시간 보정 → ssaju 계산 → 만세력 표 즉시 표시  ★ 서버 안 거침
   │ ③ localStorage에 같은 입력 있으면 → 저장된 해석 표시 (끝)
   ▼
[Netlify Function]  ← 서버 조각 1개뿐
   │ ④ 만세력 구조 데이터 + 프롬프트 → Messages API
   ▼
[Claude] → ⑤ 해석 스트리밍 → 화면에 흘림 + localStorage 저장
```

**②와 ④를 끊는 것이 핵심.** 만세력 표는 즉시 뜨고, 해석만 아래에서 채워진다. 사용자가 빈 화면을 보는 시간이 없다.

### 3.2 스택

| 레이어 | 선택 | 사유 |
| --- | --- | --- |
| 프론트 | **Vite + React + TypeScript** | 화면 2개짜리 앱에 SSR이 필요 없다. Netlify 정적 배포에 가장 잘 맞는다 |
| 스타일 | **Tailwind CSS** | 모바일 우선(mobile-first)이 기본 동작이라 §5.1 원칙과 그대로 맞는다 |
| 만세력 | **`ssaju`** (npm, MIT) | 십신·신살·지장간·진태양시 옵션 지원. 의존성 0, 약 15KB |
| AI | **Messages API** (`@anthropic-ai/sdk`) | Netlify Function에서 도는 유일한 방식 (§3.3) |
| 서버 | **Netlify Function 1개** | `/api/interpret` 하나뿐 |
| 저장 | **브라우저 localStorage** | DB 서버가 필요 없다 |
| 배포 | GitHub → Netlify 자동 배포 | push하면 알아서 올라간다 |

> Next.js로 해도 된다. 포트폴리오 사이트와 스택을 맞추고 싶으면 그 편이 낫다. 다만 이 프로젝트는 화면 2개라 Next.js의 장점(SSR·라우팅·이미지 최적화)이 쓰일 데가 없어, MVP는 가벼운 쪽으로 잡았다.

### 3.3 Agent SDK를 쓰지 않는 이유 ⚠️

초기 구상은 Claude Agent SDK + 구독(OAuth) 인증이었으나, **Netlify에서 돌지 않아 철회했다.**

Anthropic 호스팅 문서 기준:

> Agent SDK는 `claude` CLI **서브프로세스를 띄우고**, 셸·작업 디렉터리·디스크의 세션 파일을 소유한다. 호스팅은 무상태 API 래퍼를 올리는 것과 다르다.
> 권장 리소스: **에이전트 1개당 RAM 1GiB, 디스크 5GiB, CPU 1개**

Netlify Functions는 서버리스라 임시 디스크가 수백 MB뿐이다. Vercel도 같은 구조라 옮겨도 결과가 같다. **문제는 호스팅이 아니라 SDK가 컨테이너를 요구한다는 점이다.**

또한 구독 OAuth 토큰을 공개 URL 뒤에 두면 **URL을 아는 누구나 내 구독 사용량을 쓰게 되어** 약관상으로도 맞지 않는다.

→ **Messages API + API 키(종량 과금)** 로 확정.

### 3.4 비용과 안전장치

**해석 1건 = 입력 약 1,500토큰 + 출력 약 1,500토큰** (시스템 프롬프트 500 + `toCompact()` 950 / 해석문 800~1200자)

| 모델 | 입력 | 출력 | **해석 1건당** | 100건 |
| --- | --- | --- | --- | --- |
| **Haiku 4.5** | $1/MTok | $5/MTok | **약 12원** | 약 1,200원 |
| Sonnet 5 | $2/MTok | $10/MTok | 약 25원 | 약 2,500원 |
| Opus 5 | $5/MTok | $25/MTok | 약 60원 | 약 6,000원 |

*(2026.09 기준 공식 단가. 환율 1,350원 가정. 단가는 바뀔 수 있다)*

**게다가 캐싱(§6.4) 때문에 같은 사주는 딱 한 번만 호출된다.** 내 사주 + 친구 20명을 봐도 21건이다.

| 항목 | 내용 |
| --- | --- |
| 시작 모델 | **Haiku 4.5** 로 시작하고, 해석 품질이 부족하면 Sonnet 5로 올린다 (Day 5에 비교) |
| 🚨 **필수 조치** | **Anthropic 콘솔에서 월 사용 한도(spend limit)를 낮게 설정한다** (예: $5). 코드 버그로 무한 호출이 나도 여기서 끊긴다 |
| 접근 제한 | v1.0은 두지 않는다 (URL을 아는 사람만 들어옴). 남용이 보이면 §9에서 비밀번호 게이트 추가 |
| 키 보관 | Netlify 환경변수 `ANTHROPIC_API_KEY`. **코드·깃허브에 절대 넣지 않는다** (`.env`는 `.gitignore`) |

**완전 무료로 가려면** — 배포 + AI 해석을 0원으로 만드는 길은 셋뿐이고, 셋 다 대가가 있다.

| 방법 | 대가 |
| --- | --- |
| 만세력 계산기만 배포 | 배포본에 **해석 기능이 없다.** 이 제품의 절반이 사라진다 |
| Gemini 등 무료 티어 모델 사용 | 해석 품질이 달라지고, **무료 티어는 입력 데이터가 모델 개선에 쓰일 수 있다** *(미확인 — 정책 확인 필요)*. 생년월일이 들어가는 서비스라 확인 없이 쓰면 안 된다 |
| 배포하지 않고 로컬 유지 | 구독으로 0원이지만 **폰에서 못 쓴다.** PC가 켜져 있어야 하고 매번 IP로 접속해야 한다 |

> Claude 구독료로 배포까지 커버하는 길은 없다. 구독을 쓰려면 Agent SDK 경로뿐인데 그건 §3.3의 이유로 막혀 있고, 컨테이너로 옮겨도 공개 URL 뒤에 구독 토큰을 두는 것 자체가 약관에 맞지 않는다.

---

## 4. 만세력 계산 규칙 ⭐ 자르지 않는 부분

> MVP라고 여기를 줄이면 안 된다. **계산이 틀리면 이 제품은 존재 이유가 없다.**

### 4.1 계산 순서

```
입력 (양/음력, 날짜, 시각, 출생지, 성별)
 ↓ ① 음력 → 양력 변환 (음력 입력인 경우)
 ↓ ② 서머타임 보정 (해당 기간 출생자만 -1시간)
 ↓ ③ 진태양시 보정 (출생지 경도 기준)
 ↓ ④ ssaju.calculateSaju() 호출
 ↓ ⑤ 시간 미상이면 시주 항목 마스킹
결과
```

**순서가 중요하다.** ②를 먼저, ③을 나중에. 바뀌면 결과가 달라진다.

### 4.2 연·월은 절기 기준 (양력 날짜가 아님)

| 규칙 | 내용 |
| --- | --- |
| **연주** | **입춘**(양력 2월 4일 전후) 기준으로 바뀐다. 1월 1일이 아니다 |
| **월주** | 각 달의 **절입일** 기준. 매월 1일이 아니다 |

**예시**: 2000년 1월 20일생 → 입춘 전이므로 **1999년(기묘년)**. 띠는 토끼.

**필수 UI**: 1월 1일~2월 10일 출생자에게 안내 문구를 띄운다.

> "사주에서 한 해는 양력 1월 1일이 아니라 **입춘**을 기준으로 바뀝니다. 알고 계신 띠와 다르게 보일 수 있어요."

### 4.3 진태양시 보정

한국 표준시는 동경 135도 기준인데 한국은 그보다 서쪽에 있다. **보정 분 = (135 − 출생지 경도) × 4분**

| 도시 | 경도 | 보정량 |
| --- | --- | --- |
| 서울 | 126.98 | 32분 |
| 인천 | 126.71 | 33분 |
| 대전 | 127.38 | 30분 |
| 전주 | 127.15 | 31분 |
| 광주 | 126.85 | 33분 |
| 대구 | 128.60 | 26분 |
| 부산 | 129.08 | 24분 |
| 제주 | 126.53 | 34분 |

> 서울과 부산이 **8분** 차이다. 태어난 시각이 경계에 가까우면 시주가 갈린다.

| 항목 | 정책 |
| --- | --- |
| 도시 목록 | 위 **8개** (MVP). 기본값 서울 |
| 기본 동작 | 보정 **적용**. `applyLocalMeanTime: true`, `longitude: <선택 도시>` |
| 토글 | 결과 화면에 보정 ON/OFF 스위치. 끄면 즉시 재계산 (AI 재호출 없음) |
| 경계 경고 | 보정 전후로 **시주가 달라지면** 배너로 "보정 여부에 따라 시주가 달라지는 시각입니다" 안내 |

> 명리학계에서도 의견이 갈리는 부분이라 어느 쪽도 틀린 값이 아니다. 그래서 감추지 않고 토글로 드러낸다.

### 4.4 서머타임 (직접 구현)

`ssaju`가 처리하지 않으므로 해당 기간 출생자는 **입력 시각에서 1시간을 빼서** 넘긴다.

| 시행 시기 | 비고 |
| --- | --- |
| 1948~1951년 여름 | *날짜 미확정* |
| 1955~1960년 여름 | *날짜 미확정* |
| 1987~1988년 (5월 둘째 일요일 ~ 10월 둘째 일요일) | 서울올림픽 관련 |

> ⚠️ **미확인** — 위 연도는 일반적으로 알려진 값이다. **정확한 시행 일시는 한국천문연구원(KASI) 자료로 확인해 상수 테이블로 하드코딩한다.** 해당 기간 출생자에게는 "서머타임 1시간 보정됨" 배지를 노출한다.

### 4.5 시간 미상 (직접 구현)

`ssaju`는 시각 미입력 시 조용히 **12시로 계산해버린다.** 그대로 두면 **모르는 시주를 아는 척 보여주게 된다.**

| 항목 | 정책 |
| --- | --- |
| 입력 | `시간을 모릅니다` 체크박스. 체크 시 시·분 비활성화 |
| 계산 | 라이브러리엔 12시로 넘기되 내부적으로 `timeUnknown: true` 유지 |
| 화면 | **시주 칸을 `?` 로 표시.** 12시로 계산된 값은 어디에도 노출하지 않는다 |
| 파생 데이터 | 시주에서 나온 십신·신살은 **결과에서 제외**. 오행 분포도 6글자로 재집계 |
| AI 프롬프트 | "시주 정보 없음. 세 기둥만으로 해석하고 시주 관련 추측 금지" 명시 |
| 안내 | "시각을 모르면 시주를 뺀 세 기둥으로 봅니다. 큰 줄기는 유지되지만 세부는 달라질 수 있어요" |

### 4.6 자시 처리

| 구분 | 시각 | 처리 |
| --- | --- | --- |
| 야자시 | 23:00~23:59 | 당일 일주 유지 |
| 조자시 | 00:00~00:59 | 다음날 일주 |

> ⚠️ Day 1에 `ssaju` 실제 동작을 테스트해 확정하고, 다르면 보정 로직을 넣는다.

### 4.7 입력 유효성

| 항목 | 규칙 | 위반 시 |
| --- | --- | --- |
| 이름/닉네임 | 필수, 공백만 입력 불가 | "이름/닉네임을 입력해 주세요" |
| 연도 | 1900 ~ 현재 | "1900년 이후만 계산할 수 있어요" |
| 날짜 | 실재하는 날짜만 | 선택 UI에서 원천 차단 |
| 미래 | 오늘 이후 불가 | "아직 오지 않은 날짜예요" |
| 음력 윤달 | 실제 윤달이 있는 달만 체크 가능 | 없는 달은 체크박스 비활성화 |

---

## 5. 화면

### 5.1 모바일 우선 원칙 ⭐

> **모바일이 기본이고 데스크톱이 예외다.** 375px부터 그리고, 넓어질 때 무엇을 더할지 생각한다.

| 브레이크포인트 | 범위 | 전략 |
| --- | --- | --- |
| **Mobile (기준)** | ~639px | 모든 화면의 기본. 단일 컬럼 |
| Tablet | 640~1023px | 모바일 레이아웃 + 여백 확대 |
| Desktop | 1024px+ | **모바일 레이아웃을 가운데 정렬**, 최대 폭 640px |

**데스크톱을 넓히지 않는 이유**: 콘텐츠가 4열 표와 읽는 글뿐이라 넓혀서 좋아지지 않는다. 표는 셀이 벌어져 읽기 나빠지고, 글은 줄이 길면 눈이 다음 줄을 못 찾는다. 데스크톱 전용 레이아웃을 안 만들면 **개발·검수가 절반**이다.

| 항목 | 규칙 |
| --- | --- |
| 설계 기준 폭 | **375px** (좌우 패딩 16px → 콘텐츠 343px) |
| 최소 지원 | 320px (깨지지 않는 것까지) |
| 터치 영역 | 최소 **44 × 44px** |
| **hover 금지** | hover에만 있는 정보를 두지 않는다. 모바일엔 hover가 없다 |
| 화면 높이 | `100vh` 금지, **`100dvh`** 사용 |
| 하단 여백 | `env(safe-area-inset-bottom)` 반영 |
| 가로 스크롤 | **`body` 가로 스크롤 절대 금지** |
| 입력 필드 폰트 | **16px 이상** (iOS Safari가 그보다 작으면 화면을 확대해버린다) |
| 최소 폰트 | 11px |

### 5.2 입력 화면 (`/`)

| 요소 | 내용 | 필수 |
| --- | --- | --- |
| 이름/닉네임 | 텍스트 입력. 결과 화면 타이틀(`{닉네임}의 만세력`)에 쓰인다 | ✅ 공백만 입력 불가 |
| 달력 구분 | `양력` / `음력` 세그먼트 (각 44px 높이) | ✅ 기본 양력 |
| 윤달 | 체크박스 — 음력 선택 시에만 노출 | 조건부 |
| 생년월일 | 연 / 월 / 일 **각각 `<select>`** | ✅ |
| 태어난 시각 | 시 / 분 `<select>` (분은 5분 단위) | 조건부 |
| 시간 모름 | 체크박스 | — |
| 출생지 | `<select>` 8개 도시, 기본 서울 | — |
| 성별 | `남` / `여` (대운 계산에 필요) | ✅ |
| 제출 | `사주 보기` — **하단 고정(sticky)** | — |

**모바일 입력 규칙**

- 단일 컬럼 세로 스택. 한 줄에 두 필드를 나란히 두지 않는다
- 생년월일은 **네이티브 date picker를 쓰지 않는다** — 1900년까지 스크롤하는 지옥이 된다
- 커스텀 드롭다운 대신 **네이티브 `<select>`** — OS 기본 피커가 훨씬 빠르다
- 키보드가 올라오면 하단 버튼은 키보드 위로 밀리고, 포커스된 필드가 가려지지 않게 스크롤
- 입력 순서: 이름/닉네임 → 달력 → 생년월일 → 시각 → 시간 모름 → 출생지 → 성별 → 제출. **위에서 아래로 한 번에** 끝난다

### 5.2a 재진입 화면 — 저장된 정보 있을 경우 (`/`)

직전에 제출해 성공한 입력값 1건을 `localStorage`(키 `saju:lastInput`)에 저장해둔다. **AI 해석 캐시(§6.4)와는 별개**다 — 이건 "입력 폼을 다시 채울지"를 위한 저장이고, §6.4는 "AI를 다시 호출할지"를 위한 저장이다. 여러 건을 쌓는 조회 기록(§9.1 순위5, 고도화 항목)과도 다르다 — 최근 1건만 있으면 된다.

| 진입 조건 | 동작 |
| --- | --- |
| `saju:lastInput` 없음 | 평소대로 빈 입력 화면(§5.2) 표시 |
| `saju:lastInput` 있음 | 이 재진입 화면 표시. 안내 문구: `작성했던 '{닉네임}'님 정보가 있습니다.` |

| 버튼 | 동작 |
| --- | --- |
| `작성했던 정보로 확인` (주 버튼) | 저장된 입력값 그대로 계산 파이프라인(§4.1)에 넣고 **재입력 없이** 결과 화면으로 이동 |
| `새로운 정보 입력` (보조 버튼) | §5.2의 **완전히 빈** 입력 화면으로 이동. 이 시점엔 `saju:lastInput`을 지우지 않는다 — 새 폼을 제출해 성공해야만 덮어쓴다 |

### 5.3 결과 화면 (`/result`)

**순서**: 요약 헤더 → 만세력 표 → 오행 → 십신 → 신살 → **AI 해석** → 면책

#### [1] 요약 헤더

- 타이틀: `{닉네임}의 만세력` (예: `민지의 만세력`) — 입력 화면에서 받은 이름/닉네임을 그대로 채운다
- `1995.03.12 (양) 07:30 · 남 · 부산`
- `당신을 상징하는 글자: 甲(갑) — 나무` ← 해석의 기준점임을 알림
- 배지: `진태양시 -24분 (부산)` / `서머타임 보정` / `시간 미상` — **보정량을 숫자로 보여줘 근거를 드러낸다**
- 진태양시 토글

#### [2] 만세력 표 — 이 프로젝트에서 가장 까다로운 화면

```
        시주      일주      월주      연주
천간     ?        甲        己        乙
지지     ?        子        卯        亥
오행     ?      목/수     토/목    목/수
```

4열 × 3행을 375px에 넣어야 한다. 계산해보면 여유가 없다.

```
375px − 32px(패딩) = 343px
343px − 47px(라벨 열) = 296px ÷ 4열 = 74px/셀
```

| 항목 | 정책 |
| --- | --- |
| 배치 | **오른쪽부터 연→월→일→시** (전통 표기 순서) |
| 한자 크기 | **22px 고정.** 좁아도 줄이지 않는다 |
| 한글 병기 | 한자 아래 11px (`甲(갑)`) — 한자만 있으면 못 읽는다 |
| 오행 색상 | 목=초록 / 화=빨강 / 토=노랑 / 금=회색 / 수=파랑. **색상만으로 구분하지 않고 글자 병기** |
| **지장간** | **MVP에서는 표에 넣지 않는다.** 74px에 3글자를 넣으면 폰트가 9px로 내려가 하한선을 깬다 → §9로 |
| 일간 강조 | 배경색으로 강조 |
| 시간 미상 | 시주 열 전체 `?` + 흐린 처리 |
| **세로 배치 금지** | 좁다고 4열을 세로로 쌓지 않는다. 사주는 **네 기둥을 나란히** 봐야 의미가 있다 |
| 320px | 패딩 12px, 한자 20px로 축소. 그래도 4열 유지 |

#### [3] 오행 분포

- 목·화·토·금·수 5개 막대 + 개수. 343px ÷ 5 = 68px씩이라 충분
- 한 줄 요약 (`목이 많고, 금이 없어요`)
- 막대 아래 라벨은 `목` 1글자 + 개수. **색상에만 의존하지 않는다**
- 시간 미상이면 "6글자 기준" 캡션

#### [4] 십신 / [5] 신살

- 십신: 기둥별로 이름 표기 (`월주 천간: 정재`)
- 신살: 보유한 것만 태그로 나열. 자동 줄바꿈, 태그 높이 36px, 간격 8px. **없으면 섹션 비노출**
- **용어 사전 (§1.4에서 MVP로 편입)** — 십신·신살 이름을 **탭하면** 그 자리에서 한 줄 설명이 펼쳐진다. hover 아님 (§5.1)
  - 설명 문구는 **하드코딩된 사전**(십신 10개 + 주요 신살 10~15개)을 코드에 상수로 둔다. AI를 호출하지 않는다 — Day 4 단계는 AI가 아직 없다
  - 탭 영역 최소 44px 높이
  - 예: `정재: 꾸준히 쌓아 올리는 힘` / `도화살: 사람을 끄는 매력`
- 캡션: "신살은 좋고 나쁨이 아니라 성향의 특징을 가리키는 이름표입니다"

#### [6] AI 해석 — 기본 성향 및 기질 ⭐

| 항목 | 내용 |
| --- | --- |
| 형태 | 항목별 구조화, 총 800~1200자 |
| 항목 | ① 타고난 기질 ② 잘 쓰는 힘 ③ 조심할 지점 ④ 관계·소통 스타일 ⑤ 한 줄 요약 |
| 표시 | 세로 카드 스택. 제목 16px Bold, 본문 16px / 행간 1.7 |
| 로딩 | **스트리밍.** 첫 글자부터 순차 표시 |

**동작**

1. 만세력 표가 이미 떠 있는 상태에서 해석 영역만 채워진다
2. localStorage에 있으면 스트리밍 없이 즉시 전체 표시
3. **자동 스크롤**: 글이 생성되는 만큼 따라 내려가되, **사용자가 위로 스크롤하면 즉시 추적 중단** + `↓ 최신 내용 보기` 버튼
4. 실패 시 `다시 시도` 버튼 1개. **만세력 결과는 그대로 유지한다**
5. 스트리밍이 끊기면 **부분 결과를 저장하지 않는다** (불완전한 해석이 영구 고정되는 것 방지)

#### [7] 면책

> 이 해석은 만세력 계산 결과를 바탕으로 AI가 작성한 것으로, 재미와 자기 이해를 위한 참고 자료입니다. 의학적·법률적·재무적 판단의 근거로 쓰지 마세요.

---

## 6. AI 해석 설계

### 6.1 입력 데이터

`ssaju`의 **`toCompact()`** 출력(약 950토큰)을 그대로 쓴다. 이미 LLM용으로 압축된 형식이라 가공이 거의 필요 없다.

단, 우리가 조정하는 것:

- 시간 미상이면 시주 관련 항목 제거
- **대운·세운·월운 항목 제거** — 해석 범위 밖이라, 남겨두면 AI가 미래 예측으로 흘러간다

### 6.2 할루시네이션 차단 규칙 (시스템 프롬프트에 명시) ⭐

| # | 규칙 |
| --- | --- |
| 1 | `<saju_data>` 안의 정보만 근거로 쓴다. **없는 글자·신살·십신을 언급하지 않는다** |
| 2 | 각 서술이 어떤 근거에서 나왔는지 자연스럽게 드러낸다 |
| 3 | **미래를 예측하지 않는다.** 성향·기질만 서술한다 |
| 4 | 건강·질병·수명·사고를 언급하지 않는다 |
| 5 | 단정하지 않는다. "~한 편입니다", "~하는 경향이 있어요" |
| 6 | 부정적 특성은 **다른 쪽에서 강점이 되는 성질**로 서술한다 |
| 7 | 데이터가 부족하면 억지로 채우지 말고 짧게 끝낸다 |
| 8 | 존댓말, 차분한 상담사 어조. 과한 신비주의(운명, 팔자소관) 금지 |

### 6.3 프롬프트 구조

```
[시스템 프롬프트]  ← 코드에 상수로 고정. 사용자 입력이 섞이지 않는다
  역할, §6.2의 8개 규칙, 출력 형식

[사용자 메시지]
  <saju_data>{toCompact() 결과}</saju_data>
```

`temperature`는 0.3 내외로 낮춘다.

### 6.4 일관성 — localStorage 캐싱

```
저장 키 = 정규화된_양력_생년월일시 + 성별 + 시간미상여부
        + 진태양시보정여부 + (보정 ON일 때만)출생지경도 + prompt_version
```

| 항목 | 내용 |
| --- | --- |
| 저장 위치 | 브라우저 `localStorage` |
| 저장 내용 | 키 → `{ 만세력결과, 해석문, 생성일시 }` |
| 효과 | **같은 입력이면 AI를 아예 호출하지 않는다.** 일관성 + 비용 절감 |
| `prompt_version` | 프롬프트를 고치면 버전을 올려 자동으로 새 해석을 만들게 한다 |
| 한계 | **기기가 다르면 캐시가 없다.** 폰과 노트북에서 다른 해석이 나올 수 있다 → MVP에서는 수용, §9로 |

> 정규화된 **양력** 기준으로 키를 만든다. 음력으로 넣든 양력으로 넣든 같은 날이면 같은 키가 된다.

**입력 폼 재사용 저장과는 별개다** — `saju:lastInput`(§5.2a)은 "입력 폼을 다시 채울지"를 위한 저장이고, 위 캐시 키는 "AI를 다시 호출할지"를 위한 저장이다. 두 저장소는 독립적으로 동작한다.

---

## 7. 개발 순서 (약 7일)

| Day | 작업 | 완료 기준 |
| --- | --- | --- |
| **1** | **`ssaju` 검증** — 알려진 생년월일 **10건**을 기존 만세력 사이트와 대조 + 자시 동작 확인 | 10건 전부 일치. 불일치 시 라이브러리 교체 판단 |
| **1** | 프로젝트 셋업 (Vite + React + Tailwind), 깃허브 레포 → Netlify 연결 | 빈 화면이 Netlify URL로 뜬다 |
| **2** | 모바일 셸(375px 컨테이너·토큰·폰트) + 입력 화면 | 320px에서 가로 스크롤 0 |
| **3** | 계산 파이프라인 (§4.1 5단계) + 서머타임 상수 테이블 + 도시 좌표 8개 | 보정 순서대로 동작 |
| **4** | **만세력 표 — 375px부터 만든다** | 실기기 375px에서 4열 유지, 폰트 11px 이상 |
| **4** | 오행 / 십신 / 신살 + **용어 사전(탭 펼침)** + 안내 배너 3종 | 시간 미상 시 `?` 표시 |
| **4** | 🟢 **0원 배포 체크포인트** — §8.1/8.2/8.4 검수 후 Netlify 배포 | §1.4의 0원 버전 완성. 여기서 멈춰도 되고 계속해도 됨 |
| **5** | Netlify Function + Messages API + 스트리밍 | 해석 1건 수신, 첫 글자 3초 이내 |
| **5** | 시스템 프롬프트 작성 (§6.2) | 규칙 위반 0건 |
| **6** | localStorage 캐싱 + 진태양시 토글 즉시 재계산 + 에러 처리 | 재조회 시 AI 미호출 확인 |
| **7** | **검수** (§8 전체) + **실기기 확인** + 배포 | 체크리스트 통과 |

> **Day 1을 건너뛰지 않는다.** 라이브러리가 틀리면 그 위에 쌓은 전부를 다시 만들어야 한다.
>
> **실기기 확인은 시뮬레이터로 대체할 수 없다.** 폰트 렌더링·터치감·주소창 동작은 개발자도구에서 안 보인다. Netlify에 올라가 있으니 폰에서 URL로 바로 들어가면 된다.
>
> **Day 4에서 배포해봐도 된다.** §1.4 참고 — AI 없이도 완결된 화면(용어 사전 포함)이라 그대로 써보고 Day 5로 넘어갈지 정할 수 있다.

---

## 8. 검수 체크리스트

### 8.1 계산 ⭐

- [ ] 검증 케이스 10건이 기존 만세력 사이트와 일치한다
- [ ] 1~2월 초 출생자의 연주가 **전년도**로 나온다 (입춘 기준)
- [ ] 같은 생년월일시라도 서울과 부산의 보정량이 다르다 (32분 vs 24분)
- [ ] 경계 시각(07:15 등)에서 서울/부산의 시주가 실제로 갈린다
- [ ] 1988년 7월 출생에 서머타임 1시간 보정이 적용된다
- [ ] 음력 + 윤달 입력이 양력으로 정확히 변환된다
- [ ] 23:30 출생의 일주가 정해진 자시 정책대로 나온다

### 8.2 시간 미상

- [ ] 시주 칸이 `?` 이고, 12시 기준 값이 어디에도 노출되지 않는다
- [ ] 오행 분포가 6글자 기준으로 집계된다
- [ ] AI 해석문에 시주·시간 관련 서술이 없다

### 8.2a 용어 사전 (0원 체크포인트 포함)

- [ ] 십신 10개, 신살 10~15개 설명이 모두 채워져 있다 (빈 설명 없음)
- [ ] 탭하면 그 자리에서 펼쳐지고, 다시 탭하면 접힌다
- [ ] 탭 영역이 44px 이상이다
- [ ] hover 없이 탭만으로 모바일에서 전부 확인된다

### 8.3 AI 해석

- [ ] 같은 입력을 3번 조회했을 때 **완전히 동일한** 해석이 나온다
- [ ] 해석문에 계산 결과에 없는 글자·신살이 등장하지 않는다
- [ ] 미래 예측·건강·수명 서술이 없다
- [ ] 5개 항목이 모두 채워지고 800~1200자 안에 든다
- [ ] AI 호출이 실패해도 만세력 결과는 화면에 남아 있다

### 8.4 모바일 ⭐

- [ ] 320 / 375 / 414 / 768 / 1280px에서 깨지지 않는다
- [ ] **어느 폭에서도 `body` 가로 스크롤이 없다**
- [ ] 만세력 표가 375px에서 4열을 유지하고 폰트가 11px 아래로 안 내려간다
- [ ] 모든 터치 요소가 44 × 44px 이상이다
- [ ] hover에만 있는 정보가 하나도 없다
- [ ] 입력 필드 포커스 시 iOS Safari가 화면을 확대하지 않는다
- [ ] 주소창이 나타났다 사라져도 레이아웃이 안 잘린다 (`dvh`)
- [ ] 스트리밍 중 위로 스크롤하면 자동 추적이 멈춘다
- [ ] **실제 스마트폰에서 전체 흐름을 통과했다**

### 8.5 배포·보안

- [ ] `.env` 가 `.gitignore` 에 있고, 깃허브에 API 키가 올라가지 않았다
- [ ] Netlify 환경변수로 키가 주입되고 브라우저 네트워크 탭에 노출되지 않는다
- [ ] **Anthropic 콘솔에 월 사용 한도가 걸려 있다**

---

## 9. 고도화 리스트

> 버린 게 아니라 미룬 것. 다시 조사하지 않도록 검토 결론까지 남겨둔다.

### 9.1 우선순위

> **십신·신살 용어 사전은 §1.4에서 MVP(Day 4)로 편입되어 여기서 빠졌다.**

| 순위 | 항목 | 규모 | 메모 |
| --- | --- | --- | --- |
| 1 | **지장간 표시** | 소 | 표에 넣으면 375px에서 폰트가 9px로 내려간다 → `지장간 보기 ▾` 접기 + 펼치면 기둥별 세로 리스트 |
| 2 | 해석 종류 확장 (연애·직업·재물) | 중 | 프롬프트 추가 + 캐시 키에 `reading_type`. **단, 미래 예측 성격이 강해져 §6.2의 "예측 금지" 규칙을 다시 설계해야 한다** |
| 3 | 대운·세운 표시 | 중 | `ssaju`가 이미 계산한다. 시간 흐름 UI 설계 필요 |
| 4 | 서버 저장 (기기 간 캐시 공유) | 중 | localStorage는 기기별이라 폰·노트북 결과가 갈릴 수 있다. Netlify Blobs 또는 외부 DB |
| 5 | 조회 기록 목록 | 소 | localStorage에 이미 쌓이므로 화면만 만들면 된다 |
| 6 | 대화형 추가 질문 | 중 | "이 부분 더 설명해줘". Messages API 멀티턴 |
| 7 | 접근 제한 (비밀번호 게이트) | 소 | 남용이 실제로 보이면. 환경변수와 대조만 하면 끝 |
| 8 | 해외 출생지 | 소 | 도시 좌표 확장 + 시간대(UTC offset) 처리 |
| 9 | 별자리(서양점성술) 접목 | 대 | §9.2 참고 |
| 10 | 궁합 (2인 비교) | 대 | 입력·화면 구조 전면 변경 |
| 11 | 접근성 정밀 대응 (WCAG AA) | 중 | MVP는 색상+글자 병기까지만 |

### 9.2 별자리 접목 — 검토 결론 (2026.09.09)

**태양별자리 하나만 넣는 방식은 하지 않는다.** 생일로 결정되는 12개 중 하나라 **AI가 참조할 근거가 1개뿐**이다. 사주는 근거가 수십 개인데 별자리만 없으면 AI는 지어낼 수밖에 없고, §1.1 원칙과 정면 충돌한다. 별자리 파트의 낮은 품질이 사주 파트 신뢰도까지 깎는다.

**넣는다면 출생차트 수준 이상.**

| 방식 | 근거 데이터 | 판정 |
| --- | --- | --- |
| 태양별자리만 | 1개 | ❌ |
| 태양·달·상승궁 3개 | 3개 + 하우스 | ⭕ 최소 기준 |
| 출생차트 전체 | 10행성 + ASC/MC + 하우스 + 각도 | ⭕ 품질 최상 |

**라이브러리 (조사 완료)**

| 후보 | 라이선스 | 판정 |
| --- | --- | --- |
| [`circular-natal-horoscope-js`](https://github.com/0xStarcat/CircularNatalHoroscopeJS) | **Unlicense** | **1순위.** 10행성 + ASC/MC + 7종 하우스 + 각도. 순수 JS라 브라우저에서 동작 |
| [`@orrery/core`](https://github.com/rath/orrery) | **AGPL-3.0** ⚠️ | 기능은 우수하나 **웹 공개 시 사이트 소스 전체를 같은 라이선스로 공개해야 한다.** Netlify 배포 중이므로 채택 불가 |

**결합 시 지켜야 할 규칙** — 사주와 서양점성술은 서로 다른 전제의 별개 체계다. 통합 해석을 시키면 AI가 근거 없는 연결을 만든다. §6.2에 다음을 추가해야 한다.

- 두 체계를 **인과로 연결하지 않는다.** "A라서 B다"가 아니라 "A로도 보이고 B로도 보인다"
- **어긋나는 지점을 숨기지 않고 명시한다.** 억지 통합보다 정직한 편이 신뢰를 준다
- 한쪽 체계의 근거로 다른 체계를 설명하지 않는다

**이미 얻은 이득**: 별자리 검토 과정에서 출생지 좌표의 필요성이 드러나 **진태양시 정확도 개선을 MVP로 앞당겼다**(§4.3). 나중에 붙일 때 좌표 입력은 이미 되어 있다.

### 9.3 Agent SDK 재도입

컨테이너 호스팅(Railway·Render·Fly 등)으로 옮기면 Agent SDK가 돈다. 다만 **구독 OAuth 토큰으로 공개 서비스를 서빙하는 문제는 그대로 남으므로**, API 키를 쓸 거라면 Messages API로 충분하다. 재도입할 이유는 **여러 턴 대화나 도구 사용이 필요해질 때뿐**이다.

---

## 부록. 확정 필요 사항

| # | 항목 | 확인 시점 |
| --- | --- | --- |
| 1 | 서비스명 / Netlify 서브도메인 | Day 1 |
| 2 | 자시 정책 (야자시 당일/익일) | Day 1, 라이브러리 동작 확인 후 |
| 3 | 서머타임 정확한 시행 일시 | Day 3, KASI 자료 |
| 4 | 사용 모델 (Sonnet / Opus) | Day 5, 품질·단가 비교 |
| 5 | 해석 어조 (상담사 존댓말 제안) | Day 5 |
