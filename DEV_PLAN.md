# 개발 계획 — Day 1~4 (0원 배포까지)

> 근거: `SAJU_Service_PRD.md` §7(개발 순서), §8(검수 체크리스트)
> 범위: Day 4 끝 = 만세력 + 오행 + 십신 + 신살 + 용어 사전. **AI 해석(Day 5~)은 이 계획에 없음.**

## 진행 현황 요약

| Day | 상태 | 핵심 작업 |
| --- | --- | --- |
| Day 1 | ✅ 완료 | `ssaju` 라이브러리 검증 + 프로젝트 셋업 |
| Day 2 | ✅ 완료 | 모바일 셸 + 입력 화면 |
| Day 3 | 🟡 진행중 | 계산 파이프라인 (보정 로직) — 코드+자동검증 완료, 사용자 화면 확인 대기 |
| Day 4 | 🟡 진행중 | 결과 화면 완성, 배포 체크포인트/배포는 남음 |

상태는 `⬜ 미시작 / 🟡 진행중 / ✅ 완료` 로 직접 바꿔서 쓰세요.

---

## Day 1 — 라이브러리 검증 + 프로젝트 셋업

> ⚠️ 여기서 확인이 틀리면 그 위에 쌓은 전부를 다시 만들어야 함. 순서 바꾸지 말 것.

### 작업

- [x] `ssaju` 라이브러리로 알려진 생년월일 **10건** 계산 → 기존 만세력 사이트와 대조 (`scripts/verify-ssaju.mjs`, `node scripts/verify-ssaju.mjs`로 재실행 가능)
- [x] 자시 동작 확인 (23:00~23:59 야자시 / 00:00~00:59 조자시가 어떻게 나오는지) — **별도 보정 로직 불필요**, 아래 결과 참고
- [x] Vite + React + TypeScript 프로젝트 생성
- [x] Tailwind CSS 세팅 (v4, `@tailwindcss/vite` 플러그인)
- [x] GitHub 레포 생성 → Netlify 연결 (`github.com/yminji117/mingming_saju` → `mingming-saju.netlify.app`)

### 검증 결과 요약

- 일주(mod-60 앙커 1984-01-31=甲子일 기준) 7건, 연주(공식 검증) 5건, 시주(시두법 공식) 9건 — **전부 일치**
- 입춘 정밀 경계: 2000년 입춘(2/4 21:40) 기준 40분 전(21:00)엔 1999년(己卯), 20분 후(22:00)엔 2000년(庚辰)으로 정확히 갈림 → **ssaju가 정밀 절입시각을 쓴다는 것 확인**
- 자시 정책 실측: 23:30(야자시)은 **당일 일주 유지**, 00:30(조자시)은 **그날 일주 그대로** → PRD §4.6에 적어둔 정책과 정확히 일치, 앱에서 추가 보정 코드 불필요
- 음력 입력 스모크 테스트(2023년 음력 2월 15일) 에러 없이 계산됨 — 정답 대조는 향후 별도 확인

### 완료 기준

- [x] 검증 케이스 10건 전부 일치
- [x] 빈 화면이 Netlify URL로 뜬다 (`mingming-saju.netlify.app`, HTML 셸 서빙 확인 — 실기기 육안 확인은 사용자 진행 중)

### 확정 필요 사항 (PRD 부록)

- [x] 서비스명 / Netlify 서브도메인 정하기 — `mingming_saju` / `mingming-saju.netlify.app`
- [x] 자시 정책(야자시 당일/익일) — ssaju 기본 동작이 PRD 정책과 일치, 별도 처리 불필요
- [x] 배포처 확정 — Netlify (Vercel과 비교 후 확정: 무료 티어 상업적 이용 허용이 결정 요인)

---

## Day 2 — 모바일 셸 + 입력 화면

### 작업

- [x] 375px 기준 모바일 컨테이너 / 디자인 토큰 / 폰트 세팅 (Mona10/12 `@font-face` 적용, `max-w-[345px]` 컨테이너, `dvh`+safe-area 패딩)
- [x] 입력 화면(`/`) 구성: 이름/닉네임 → 양력/음력 세그먼트 → 생년월일 select → 시각 select → 시간 모름 체크 → 출생지 select(8개 도시) → 성별 → 제출 버튼
- [x] 네이티브 `<select>` 사용 (커스텀 드롭다운·date picker 금지)
- [x] 입력 유효성: 1900년~오늘, 실재 날짜만, 음력 윤달은 실제 윤달 있는 달만 (`lib/validation.ts`, `lib/lunar.ts` — ssaju로 윤달 유효성 판별)
- [x] Figma `2:5` 기준 픽셀 스타일 적용 (`Bezel`/`PixelButton` 컴포넌트, `skill/pixel-design-skill.md` 참고)
- [x] 닉네임 입력 필수 검증 (빈 값/공백만 입력 시 제출 차단, PRD §4.7)
- [x] "저장된 정보 있을 경우" 분기 화면 (Figma `8:32`, PRD §5.2a) — `App.tsx`의 `EntryRoute`가 `saju:lastInput` 존재 여부로 분기
- [x] 보조 버튼 컴포넌트 추가 (검정 배경 + 흰 보더, `PixelButton variant="secondary"`)

### 디자인 정합성 업데이트 (Figma 재확인 후 반영)

Figma가 Day2 작업 이후 갱신되어(성별 위치, 양력/음력/윤달 체크박스 3개로 변경, 실제 버튼/하트/체크 아이콘 에셋 추가 등) 실제 에셋을 다시 받아 화면을 재구성했다. **결과 화면은 이번 범위에서 제외.**

- 실제 Figma 에셋 사용: 하트 아이콘(`assets/heart.png`), 체크 아이콘(`assets/check-icon.svg`), 버튼 노치 배경(`assets/button-primary.svg` #F31BC0, `assets/button-secondary.svg` 검정) — CSS 근사 대신 진짜 이미지로 교체
- 필드 순서 변경: 닉네임 → 성별(여자/남자, 핑크/흰색 박스) → 생년월일 → 양력/음력/윤달(체크박스 3개, 상호배타) → 시각 → 시간모름 → 출생지
- 도시 표시명을 정식 행정구역명으로 (`서울` → `서울특별시` 등, 내부 로직 키는 그대로 유지)
- 저장된 정보 화면 안내 문구를 Figma 그대로 반영: `작성했던 [닉네임]님의 정보가 있습니다.` (닉네임은 핑크 볼드, Pretendard 폰트 — Figma가 이 한 줄만 Pretendard를 쓰길래 그대로 따름)
- 미사용 컴포넌트(`SegmentToggle`) 삭제

### 구현 메모

- 라우팅: react-router-dom 추가 (`/` 입력 화면, `/result` — Day4 전이라 입력값 확인용 스텁 화면)
- 제출(`확인하기`) 시 실제 사주 계산은 아직 안 함 (Day3 계산 파이프라인 이후). 지금은 입력값이 `saju:lastInput`에 저장되고 `/result` 스텁에 그대로 표시되는 것까지 확인
- 하드 오프셋 그림자 버튼은 Figma의 SVG 톱니 테두리 대신 CSS `box-shadow`로 근사함(`ponytail:` 주석 남김) — 노치 모양이 꼭 필요해지면 그때 SVG로 교체
- 음력 일자 select는 29/30일 여부를 정확히 좁히지 않고 30일까지 항상 노출 (`ponytail:` 주석) — 실제 없는 날짜는 제출 시 validateForm이 걸러냄

### 완료 기준

- [x] 320px 폭에서 가로 스크롤 0 (Playwright로 확인: `scrollWidth === clientWidth === 320`, 콘솔 에러 없음, `scripts/check-mobile.mjs`로 재확인 가능)

---

## Day 3 — 계산 파이프라인

### 작업

- [x] 음력→양력 변환 처리 (`lib/sajuPipeline.ts` — ssaju `lunarToSolar()` 사용, PRD §4.1 순서대로 가장 먼저 실행)
- [x] 서머타임 상수 테이블 하드코딩 (`lib/dst.ts`) — KASI 자료 대신 **IANA tzdata(Asia/Seoul)** 실제 전환 시각을 `zdump -v`로 뽑아서 사용 (더 정확함, 아래 참고)
- [x] 진태양시 보정 로직: ssaju의 `applyLocalMeanTime`/`longitude` 내장 옵션을 그대로 사용 (직접 구현 불필요 — 실측으로 서울 -32.08분/부산 -23.68분 확인됨)
- [x] 8개 도시 좌표 테이블 — Day2에서 이미 작성한 `lib/cities.ts` 재사용
- [x] 계산 순서 준수: 음력변환 → 서머타임 보정 → 진태양시 보정(ssaju 내장) → `ssaju.calculateSaju()` (`lib/sajuPipeline.ts`의 `computeSaju()`)
- [x] 시간 미상 처리: 12시로 계산하되 `timeUnknown: true` 반환, 서머타임/진태양시 보정은 건너뜀 (시각이 가짜라 의미 없음). `/result` 스텁에서 시주를 `?`로 마스킹

### 발견 사항 (PRD에 없던 것)

- ssaju는 `timezone: "Asia/Seoul"`을 넘겨도 **서머타임을 자동 보정하지 않음** — 실측으로 확인(申시 vs 未시). PRD 가정이 맞았고, 직접 구현이 필요했음
- 1954.3.20~1961.8.9 사이 한국 표준시 자체가 UTC+8:30이었던 시기가 있음(tzdata 확인). PRD·이 파이프라인 모두 이 부분은 다루지 않음 — 해당 기간 출생자가 실제로 있을 수 있어 **Day4 전 확인 필요** 항목으로 남겨둠

### 완료 기준

- [x] §4.1 순서대로 보정이 동작한다 — `scripts/verify-pipeline.mjs`로 자동 검증 (서머타임 적용/미적용 대조군, 서울·부산 보정량, 경계시각 시주 분기, 음력+윤달 변환, 시간미상 처리 — 6개 항목 전부 통과)
- [ ] **사용자 브라우저 확인** — 입력 화면에서 실제로 제출해서 `/result`에 계산 결과가 뜨는지 확인 (아래 안내 참고)

### 확정 필요 사항

- [x] 서머타임 정확한 시행 일시 — KASI 대신 IANA tzdata(Asia/Seoul)로 확인, `lib/dst.ts`에 반영

---

## Day 4 — 결과 화면 + 0원 배포

### 작업

- [x] 요약 헤더: 생년월일시/성별/출생지 (진태양시/서머타임 배지·토글은 Figma 재설계(`48:224`)에서 완전히 빠져 UI에서 제거 — 보정 계산 자체는 항상 그대로 적용됨)
- [x] **일주 배지**: 일간 오행 색상 단어(초록/빨간/노란/흰/검정) + 십이지 동물(쥐~돼지) 조합 문구(예: "검정 호랑이") + 천간 캐릭터 아이콘, `lib/elements.ts`의 `ELEMENT_COLOR_WORD`/`BRANCH_ANIMAL`로 매핑
- [x] 만세력 표 — Figma 재설계로 5행 구조(헤더 라벨 → 천간-십신 → 천간 → 지지 → 지지-십신)로 교체, 좌측 라벨 열 없이 4열 균등폭. 오행은 별도 행 대신 천간/지지 글자 색상으로 표현
- [x] 오행 분포 막대 (5개, 색상+글자 병기, 시간 미상 시 "6글자 기준" 캡션. "~많고 ~없어요" 요약 문장은 Figma에 없어 제거)
- [x] 십신 / 신살 표시 — Figma 재설계로 탭-확장 방식(`GlossaryTerm`, 삭제됨) 대신 **모달 팝업**(`DetailModal`) 방식으로 전환. "자세히 보기" 링크 → 십신 모달, 신살 (i) 아이콘 → 신살 모달. 신살 본문 표는 `ssaju`가 실제로 계산하는 값(12신살+특수신살)만 기둥별로 나열 (Figma 목업에 있던 암록·양인살 등 11개는 라이브러리가 계산 못 하는 예시 텍스트로 판단해 제외)
- [x] **용어 사전** — 십신 10개 + 신살 14개(`lib/glossary.ts`, 변경 없음) 문구가 두 모달(34:173/48:1004) 내용과 정확히 일치함을 확인
- [x] 안내 배너 3종: 입춘 기준 연도 안내 / 진태양시 보정 경계 안내 / 시간 미상 안내(조건부)
- [x] `.env`를 `.gitignore`에 등록 — 이미 되어 있었음 (Day1에서 처리)
- [x] **천간 10개 캐릭터 에셋 완료** — Figma `48:1206`에서 경금(칼)·신금(크리스탈) 아이콘을 새로 받아 `src/assets/characters/`에 추가, `lib/characters.ts`의 플레이스홀더 분기 제거. 8/10 → **10/10**
- [x] 결과 화면 타이틀이 `{닉네임}의 만세력`로 동적 치환된다 (PRD §5.3 [1])
- [x] `작성했던 정보로 확인` → 저장값(`saju:lastInput`)으로 재입력 없이 결과 화면 진입 확인
- [x] `새로운 정보 입력` → 완전히 빈 폼으로 이동, 기존 저장값은 유지되는지(새로고침해도 안 지워짐) 확인 — Day2에 이미 구현된 로직, 결과 화면 교체로 영향 없음

### 완료 기준 (화면 구현)

- [x] 320px/375px 가로 스크롤 0, 콘솔 에러 없음 (Playwright로 재확인)
- [x] 시간 미상 입력 시 시주 `?` 처리(표 4행 전부) + 오행 6글자 기준 캡션 + 신살에서 시주 제외 확인
- [x] 십신/신살 모달 열기 → 내용 확인 → X 아이콘으로 닫기 확인
- [x] 일주 배지 색상+동물 조합이 오행 5종(목/화/토/금/수) 전부에서 올바르게 나온다 (경금·신금 새 아이콘 포함 실측 확인)

> AI 해석(§5.3 [6])과 면책(§5.3 [7])은 이 계획 범위 밖(Day 5~)이라 포함하지 않음.

### 🟢 0원 배포 체크포인트

배포 전 아래 항목(PRD §8 중 Day4 해당분)을 먼저 확인하세요.

**계산 (§8.1)**
- [ ] 검증 케이스 10건이 기존 만세력 사이트와 일치한다
- [ ] 1~2월 초 출생자의 연주가 전년도로 나온다 (입춘 기준)
- [ ] 같은 생년월일시라도 서울/부산의 보정량이 다르다 (32분 vs 24분)
- [ ] 경계 시각(07:15 등)에서 서울/부산의 시주가 실제로 갈린다
- [ ] 1988년 7월 출생에 서머타임 1시간 보정이 적용된다
- [ ] 음력 + 윤달 입력이 양력으로 정확히 변환된다
- [ ] 23:30 출생의 일주가 정해진 자시 정책대로 나온다

**시간 미상 (§8.2)**
- [ ] 시주 칸이 `?` 이고, 12시 기준 값이 어디에도 노출되지 않는다
- [ ] 오행 분포가 6글자 기준으로 집계된다

**용어 사전 (§8.2a)**
- [ ] 십신 10개, 신살 10~15개 설명이 모두 채워져 있다 (빈 설명 없음)
- [ ] 탭하면 펼쳐지고 다시 탭하면 접힌다
- [ ] 탭 영역이 44px 이상이다
- [ ] hover 없이 탭만으로 모바일에서 전부 확인된다

**모바일 (§8.4)**
- [ ] 320 / 375 / 414 / 768 / 1280px에서 깨지지 않는다
- [ ] 어느 폭에서도 `body` 가로 스크롤이 없다
- [ ] 만세력 표가 375px에서 4열을 유지하고 폰트가 11px 아래로 안 내려간다
- [ ] 모든 터치 요소가 44×44px 이상이다
- [ ] 입력 필드 포커스 시 iOS Safari가 화면을 확대하지 않는다
- [ ] 주소창이 나타났다 사라져도 레이아웃이 안 잘린다 (`dvh` 사용)
- [ ] **실제 스마트폰에서 전체 흐름을 통과했다**

### 배포

- [ ] Netlify에 배포 → URL로 접속 확인
- [ ] 여기서 멈춰도 되고, 계속해서 Day 5(AI 해석)로 넘어가도 됨

---

## 참고 — 이 계획에 없는 것 (Day 5~7)

AI 해석(Netlify Function, Messages API, 스트리밍, 프롬프트, localStorage 캐싱, API 키 관리)은 이번 범위 밖입니다. Day 4 배포 후 계속할지 결정할 때 PRD §7의 Day 5~7 항목을 참고하세요.
