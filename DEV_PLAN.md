# 개발 계획 — Day 1~4 (0원 배포까지)

> 근거: `SAJU_Service_PRD.md` §7(개발 순서), §8(검수 체크리스트)
> 범위: Day 4 끝 = 만세력 + 오행 + 십신 + 신살 + 용어 사전. **AI 해석(Day 5~)은 이 계획에 없음.**

## 진행 현황 요약

| Day | 상태 | 핵심 작업 |
| --- | --- | --- |
| Day 1 | ⬜ 미시작 | `ssaju` 라이브러리 검증 + 프로젝트 셋업 |
| Day 2 | ⬜ 미시작 | 모바일 셸 + 입력 화면 |
| Day 3 | ⬜ 미시작 | 계산 파이프라인 (보정 로직) |
| Day 4 | ⬜ 미시작 | 결과 화면 + 0원 배포 |

상태는 `⬜ 미시작 / 🟡 진행중 / ✅ 완료` 로 직접 바꿔서 쓰세요.

---

## Day 1 — 라이브러리 검증 + 프로젝트 셋업

> ⚠️ 여기서 확인이 틀리면 그 위에 쌓은 전부를 다시 만들어야 함. 순서 바꾸지 말 것.

### 작업

- [x] `ssaju` 라이브러리로 알려진 생년월일 **10건** 계산 → 기존 만세력 사이트와 대조 (`scripts/verify-ssaju.mjs`, `node scripts/verify-ssaju.mjs`로 재실행 가능)
- [x] 자시 동작 확인 (23:00~23:59 야자시 / 00:00~00:59 조자시가 어떻게 나오는지) — **별도 보정 로직 불필요**, 아래 결과 참고
- [x] Vite + React + TypeScript 프로젝트 생성
- [x] Tailwind CSS 세팅 (v4, `@tailwindcss/vite` 플러그인)
- [ ] GitHub 레포 생성 → Netlify 연결 (gh/netlify CLI 미설치 — 사용자 확인 필요)

### 검증 결과 요약

- 일주(mod-60 앙커 1984-01-31=甲子일 기준) 7건, 연주(공식 검증) 5건, 시주(시두법 공식) 9건 — **전부 일치**
- 입춘 정밀 경계: 2000년 입춘(2/4 21:40) 기준 40분 전(21:00)엔 1999년(己卯), 20분 후(22:00)엔 2000년(庚辰)으로 정확히 갈림 → **ssaju가 정밀 절입시각을 쓴다는 것 확인**
- 자시 정책 실측: 23:30(야자시)은 **당일 일주 유지**, 00:30(조자시)은 **그날 일주 그대로** → PRD §4.6에 적어둔 정책과 정확히 일치, 앱에서 추가 보정 코드 불필요
- 음력 입력 스모크 테스트(2023년 음력 2월 15일) 에러 없이 계산됨 — 정답 대조는 향후 별도 확인

### 완료 기준

- [x] 검증 케이스 10건 전부 일치
- [ ] 빈 화면이 Netlify URL로 뜬다 (Netlify 연결 전이라 미완료)

### 확정 필요 사항 (PRD 부록)

- [ ] 서비스명 / Netlify 서브도메인 정하기
- [x] 자시 정책(야자시 당일/익일) — ssaju 기본 동작이 PRD 정책과 일치, 별도 처리 불필요

---

## Day 2 — 모바일 셸 + 입력 화면

### 작업

- [ ] 375px 기준 모바일 컨테이너 / 디자인 토큰 / 폰트 세팅
- [ ] 입력 화면(`/`) 구성: 양력/음력 세그먼트 → 생년월일 select → 시각 select → 시간 모름 체크 → 출생지 select(8개 도시) → 성별 → 제출 버튼(하단 고정)
- [ ] 네이티브 `<select>` 사용 (커스텀 드롭다운·date picker 금지)
- [ ] 입력 유효성: 1900년~오늘, 실재 날짜만, 음력 윤달은 실제 윤달 있는 달만
- [ ] Figma `2:5` 기준 픽셀 스타일 적용 (`skill/pixel-design-skill.md` 참고 — 다크 배경, Mona 픽셀 폰트, 하드 베젤 입력창)
- [ ] 닉네임 입력 필수 검증 (빈 값/공백만 입력 시 제출 차단, PRD §4.7)
- [ ] "저장된 정보 있을 경우" 분기 화면 (Figma `8:32`, PRD §5.2a) — 진입 시 `saju:lastInput` 존재 여부로 Main ↔ 이 화면 분기
- [ ] 보조 버튼 컴포넌트 추가 (검정 배경 + 흰 보더, "새로운 정보 입력" 스타일)

### 완료 기준

- [ ] 320px 폭에서 가로 스크롤 0

---

## Day 3 — 계산 파이프라인

### 작업

- [ ] 음력→양력 변환 처리
- [ ] 서머타임 상수 테이블 하드코딩 (1948~51, 1955~60, 1987~88 — KASI 자료로 정확한 날짜 확인)
- [ ] 진태양시 보정 로직: `보정 분 = (135 − 출생지 경도) × 4분`
- [ ] 8개 도시 좌표 테이블 (서울/인천/대전/전주/광주/대구/부산/제주)
- [ ] 계산 순서 준수: 음력변환 → 서머타임 보정 → 진태양시 보정 → `ssaju.calculateSaju()` → 시간 미상이면 시주 마스킹
- [ ] 시간 미상 처리: 라이브러리엔 12시로 넘기되 `timeUnknown: true` 별도 유지, 12시 값은 화면에 노출 금지

### 완료 기준

- [ ] §4.1 순서대로 보정이 동작한다 (순서 바뀌면 결과 달라짐 — 특히 서머타임 먼저, 진태양시 나중)

### 확정 필요 사항

- [ ] 서머타임 정확한 시행 일시 — KASI 자료로 확인

---

## Day 4 — 결과 화면 + 0원 배포

### 작업

- [ ] 요약 헤더: 생년월일시/성별/출생지, 일간 강조, 보정 배지(진태양시/서머타임/시간미상), 진태양시 토글
- [ ] 만세력 표 (4열×3행, 375px 안에 맞춤 — 한자 22px 고정, 한글 병기 11px, 오행 색상+글자 병기, 일간 배경 강조, 지장간은 넣지 않음)
- [ ] 오행 분포 막대 (5개, 색상+글자 병기, 시간 미상 시 "6글자 기준" 캡션)
- [ ] 십신 / 신살 표시 (신살 없으면 섹션 숨김)
- [ ] **용어 사전** — 십신 10개 + 신살 10~15개 하드코딩 상수, 탭하면 한 줄 설명 펼침 (hover 아님, 탭 영역 44px 이상)
- [ ] 안내 배너 3종: 입춘 기준 연도 안내 / 진태양시 보정 경계 안내 / 시간 미상 안내
- [ ] `.env`를 `.gitignore`에 등록 (API 키는 아직 없지만 습관 고정)
- [ ] 헤더에 마스코트 캐릭터 카드 추가 (일간 기준 1개, Figma `5:5` 참고 — 기존 표/오행/십신/신살 구성은 그대로 유지, 카드는 추가 요소)
- [ ] 천간 10개 캐릭터 에셋 확보 (현재 8/10 — 경금·신금 제작 필요, `skill/pixel-design-skill.md` §6 참고)
- [ ] 결과 화면 타이틀이 `{닉네임}의 만세력`로 동적 치환된다 (PRD §5.3 [1])
- [ ] `작성했던 정보로 확인` → 저장값(`saju:lastInput`)으로 재입력 없이 결과 화면 진입 확인
- [ ] `새로운 정보 입력` → 완전히 빈 폼으로 이동, 기존 저장값은 유지되는지(새로고침해도 안 지워짐) 확인

### 🟢 0원 배포 체크포인트

배포 전 아래 항목(PRD §8 중 Day4 해당분)을 먼저 확인하세요.

**계산 (§8.1)**
- [ ] 검증 케이스 10건이 기존 만세력 사이트와 일치한다
- [ ] 1~2월 초 출생자의 연주가 전년도로 나온다 (입춘 기준)
- [ ] 같은 생년월일시라도 서울/부산의 보정량이 다르다 (32분 vs 24분)
- [ ] 경계 시각(07:15 등)에서 서울/부산의 시주가 실제로 갈린다
- [ ] 1988년 7월 출생에 서머타임 1시간 보정이 적용된다
- [ ] 음력 + 윤달 입력이 양력으로 정확히 변환된다
- [ ] 23:30 출생의 일주가 정해진 자시 정책대로 나온다

**시간 미상 (§8.2)**
- [ ] 시주 칸이 `?` 이고, 12시 기준 값이 어디에도 노출되지 않는다
- [ ] 오행 분포가 6글자 기준으로 집계된다

**용어 사전 (§8.2a)**
- [ ] 십신 10개, 신살 10~15개 설명이 모두 채워져 있다 (빈 설명 없음)
- [ ] 탭하면 펼쳐지고 다시 탭하면 접힌다
- [ ] 탭 영역이 44px 이상이다
- [ ] hover 없이 탭만으로 모바일에서 전부 확인된다

**모바일 (§8.4)**
- [ ] 320 / 375 / 414 / 768 / 1280px에서 깨지지 않는다
- [ ] 어느 폭에서도 `body` 가로 스크롤이 없다
- [ ] 만세력 표가 375px에서 4열을 유지하고 폰트가 11px 아래로 안 내려간다
- [ ] 모든 터치 요소가 44×44px 이상이다
- [ ] 입력 필드 포커스 시 iOS Safari가 화면을 확대하지 않는다
- [ ] 주소창이 나타났다 사라져도 레이아웃이 안 잘린다 (`dvh` 사용)
- [ ] **실제 스마트폰에서 전체 흐름을 통과했다**

### 배포

- [ ] Netlify에 배포 → URL로 접속 확인
- [ ] 여기서 멈춰도 되고, 계속해서 Day 5(AI 해석)로 넘어가도 됨

---

## 참고 — 이 계획에 없는 것 (Day 5~7)

AI 해석(Netlify Function, Messages API, 스트리밍, 프롬프트, localStorage 캐싱, API 키 관리)은 이번 범위 밖입니다. Day 4 배포 후 계속할지 결정할 때 PRD §7의 Day 5~7 항목을 참고하세요.
